<div class="comments">
  {{#each this}}
    <section class="comment">
      <div class="meta">
        {{user.username}}
        on
        {{format_date date_posted}}
      </div>
      <div class="text">
        {{comment.content}}
      </div>
    </section>
  {{/each}}
</div>